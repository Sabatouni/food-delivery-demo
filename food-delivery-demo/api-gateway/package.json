{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway — single entry point that routes requests to microservices",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
